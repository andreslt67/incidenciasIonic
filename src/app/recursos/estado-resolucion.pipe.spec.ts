import { EstadoResolucionPipe } from './estado-resolucion.pipe';

describe('EstadoResolucionPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoResolucionPipe();
    expect(pipe).toBeTruthy();
  });
});
